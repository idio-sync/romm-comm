# Runtime dependencies, pinned. Bump deliberately, not by rebuild.
py-cord==2.6.1
aiohttp==3.12.15
python-dotenv==1.1.1
qrcode==8.2
Pillow==11.3.0
python-socketio[asyncio_client]==5.13.0
aiosqlite==0.21.0
python-dateutil==2.9.0.post0

# Removed: requests, docker, websockets. None were imported anywhere in the
# tree and nothing in the dependency graph pulls them in. The asyncio SocketIO
# client speaks WebSocket over aiohttp, so the `websockets` package is not
# needed for scan events.
